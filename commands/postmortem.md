---
description: "Author a formal incident postmortem (shortcut for /task postmortem)"
---

User invoked /postmortem. Route to /task postmortem — read skills/tasks/postmortem.md and execute that template, gathering required inputs (incident title, date, severity, detection signal, resolution signal). Engage /template postmortem-doc if not already active. Save to <repo>/postmortems/. List action items separately and suggest /todo add for each.
