---
description: "Reasoning effort (e.g. /depth quick, /depth thorough, /depth off, alone to list)"
---

User typed: /depth $ARGUMENTS. Hook updated the active flag.

If empty: list available modes from skills/depth/*/SKILL.md.

If 'off': confirm — reverts to medium (default Claude reasoning).

Otherwise: confirm depth mode is active. Reminder: quick = first-pass; medium = default with 2-3 alternatives; thorough = full deliberation with explicit weighing.
