---
description: "Closed-loop delivery (e.g. /acceptance strict, /acceptance light, /acceptance off)"
---

User typed: /acceptance $ARGUMENTS. Hook updated flag.

If empty: show state.

If 'off': confirm.

Otherwise (strict|light): confirm mode. Reminder: strict = every non-trivial task gets explicit criteria before execute; light = same for medium+ tasks only. After 'done', verify against criteria explicitly.
