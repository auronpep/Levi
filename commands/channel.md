---
description: "Reasoning lens (e.g. /channel security, /channel cost, /channel ux, /channel off, alone to list)"
---

User typed: /channel $ARGUMENTS. Hook updated active flag.

If empty: list available channels from skills/channel/*/SKILL.md.

If 'off': confirm — no specific lens active.

Otherwise: confirm the channel is active. Reminder: channel narrows reasoning angle (security/cost/ux/performance/reliability/data). Compose with /role for full responsibilities, /depth thorough for explicit weighing through this lens.
