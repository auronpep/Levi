---
description: "Output verbosity (e.g. /granularity terse, /granularity expansive, /granularity off, alone to list)"
---

User typed: /granularity $ARGUMENTS. Hook updated the active flag.

If empty: list available modes from skills/granularity/*/SKILL.md.

If 'off': confirm — defaults back to normal.

Otherwise: confirm the granularity mode is active. Reminder of length targets per mode (terse ~50-150 words, normal ~300-500, expansive ~800-1500).
