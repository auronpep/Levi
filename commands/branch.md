---
description: "Try multiple approaches in parallel (e.g. /branch 3, /branch off)"
---

User typed: /branch $ARGUMENTS. Hook updated active flag.

If empty: show current state.

If 'off': confirm — single-approach mode.

Otherwise (a number N): confirm parallel-approach mode with N parallel attempts. For substantive tasks: decompose into N angles, spawn N subagents in parallel, reconcile, recommend best. Cost = N× per task; use deliberately.
